id: com.acme.wasm-demo
version: 0.1.0
display_name: "Wasm demo block"
description: "Reference Wasm block — two nodes, `double` (doubles a number) and `add` (sums two numbers)."

contributes:
  wasm_modules:
    - path: dist/block.wasm
  kinds: []

requires:
  - id: spi.msg
    version: "^1"
